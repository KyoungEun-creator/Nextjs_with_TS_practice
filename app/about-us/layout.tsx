export default function AboutUsLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {children}
      &copy; This is a Layout only for AboutUS page! How cool!
    </div>
  );
}
